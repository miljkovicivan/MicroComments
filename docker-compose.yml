version: '2'

services:

  comments:
    build: .
    #tty: true
    #command: bash
    command: ./docker/run.sh
    ports:
      - "5000:5000"
    volumes:
      - .:/app
    links:
      - mongodb
    environment:
      - ENV=local

  mongodb:
    image: mongo:3.4.4
    volumes:
     - ./tmp-docker-vol/mongodb:/data/db


